How to run DataPrepper:

- open the DataPrepper folder
- open DataPrepper.sln
- then start without debugging (requires vs community);

How to run CardReaderAI

- (Requires Node.js);
- In terminal of App.js, type in "node app.js". App.js may be editted to test different functions, but not the other scripts please.
